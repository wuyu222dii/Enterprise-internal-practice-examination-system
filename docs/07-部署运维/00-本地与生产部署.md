# 部署与运维（MVP）

| 文档属性 | 内容 |
| --- | --- |
| 文档版本 | V0.1 |
| 编制日期 | 2026-08-30 |

---

## 1. 本地开发环境

```bash
docker compose up -d          # PostgreSQL:5433 Redis:6380 MinIO:19000/19001
cd backend && mvn spring-boot:run   # API :8088/api/v1
cd web-admin && npm run dev   # :5173
cd web-exam && npm run dev    # :5174
```

## 2. 非功能目标（需求 §17）

| 指标 | 目标 |
| --- | --- |
| 单场并发答题 | 500 人 |
| 答案保存 P95 | ≤ 1s |
| 开卷/交卷 P95 | ≤ 3s |
| RPO | 0（已确认写入） |
| RTO | ≤ 1 小时 |
| 数据保留 | 5 年 |

## 3. 压测

```bash
k6 run scripts/load-test/login-smoke.js
# PERF-01~03 完整脚本待 M8 补充
```

## 4. 生产部署（待细化）

- 内网单机或主从 PostgreSQL
- Redis 会话与幂等
- MinIO / 对象存储存放 Excel 与导出文件
- 反向代理 TLS 终结
